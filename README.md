

Solution 2: Use Python 3.11 or 3.12 (Alternative)
If you still have issues, create a new environment with Python 3.11:

bash
# Using conda (since you have anaconda)
conda create -n veilarmor python=3.11 -y
conda activate veilarmor

# Install requirements
pip install -r requirements.txt